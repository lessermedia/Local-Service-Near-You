/** @type {import('next').NextConfig} */
const nextConfig = {
  // eslint and typescript error ignoring removed for strict build checks
  images: {
    unoptimized: true,
  },
}

export default nextConfig